app = "dnzx-lacasamods"
primary_region = "gig"

[build]
  dockerfile = "Dockerfile"

[services]
  internal_port = 8080
  protocol = "tcp"

[deploy]
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1
  
